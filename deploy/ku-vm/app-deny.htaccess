# Block web access to Laravel app when it lives under ~/html/ku-phumpanya-app
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
